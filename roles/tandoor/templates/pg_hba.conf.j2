# TYPE  DATABASE        USER            ADDRESS                 METHOD

# "local" is for Unix domain socket connections only
local   all             all                                     trust

# IPv4 local connections (Docker network)
host    all             all             0.0.0.0/0               md5

# IPv6 local connections (Docker network)
host    all             all             ::/0                    md5
