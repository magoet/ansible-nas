---
- name: Verify
  hosts: all
  gather_facts: false
  tasks:
    - ansible.builtin.include_vars:
        file: ../../defaults/main.yml

    - name: Try and stop and remove OpenClaw
      community.docker.docker_container:
        name: "{{ openclaw_container_name }}"
        state: absent
      register: result

    - name: Check OpenClaw is stopped
      ansible.builtin.assert:
        that:
          - not result.changed